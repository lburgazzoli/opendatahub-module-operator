apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: opendatahub-module-operator-serving-cert
  namespace: {{ .Release.Namespace }}
spec:
  dnsNames:
  - opendatahub-module-operator-webhook-service.{{ .Release.Namespace }}.svc
  - opendatahub-module-operator-webhook-service.{{ .Release.Namespace }}.svc.cluster.local
  issuerRef:
    kind: Issuer
    name: opendatahub-module-operator-selfsigned-issuer
  secretName: webhook-server-cert