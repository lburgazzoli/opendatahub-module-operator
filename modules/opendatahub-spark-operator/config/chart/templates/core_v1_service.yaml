apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: opendatahub-spark-operator
  name: odh-spark-metrics-service
  namespace: {{ .Release.Namespace }}
spec:
  ports:
  - name: https
    port: 8443
    protocol: TCP
    targetPort: 8443
  selector:
    app.kubernetes.io/name: opendatahub-spark-operator