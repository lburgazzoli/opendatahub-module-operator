namespace: opendatahub-mlflow-operator-system

namePrefix: opendatahub-mlflow-operator-

resources:
- ../crd
- ../rbac
- ../manager
- metrics_service.yaml

patches:
- path: manager_metrics_patch.yaml
  target:
    kind: Deployment
