# This rule is not used by the project opendatahub-mymodule-operator itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants read-only access to components.platform.opendatahub.io resources.
# This role is intended for users who need visibility into these resources
# without permissions to modify them. It is ideal for monitoring purposes and limited-access viewing.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: opendatahub-mymodule-operator
    app.kubernetes.io/managed-by: kustomize
  name: components-mymodule-viewer-role
rules:
- apiGroups:
  - components.platform.opendatahub.io
  resources:
  - mymodules
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - components.platform.opendatahub.io
  resources:
  - mymodules/status
  verbs:
  - get
