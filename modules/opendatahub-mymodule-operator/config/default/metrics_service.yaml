apiVersion: v1
kind: Service
metadata:
  labels:

    app.kubernetes.io/name: opendatahub-mymodule-operator
    app.kubernetes.io/managed-by: kustomize
  name: manager-metrics
  namespace: system
spec:
  ports:
  - name: https
    port: 8443
    protocol: TCP
    targetPort: 8443
  selector:

    app.kubernetes.io/name: opendatahub-mymodule-operator
